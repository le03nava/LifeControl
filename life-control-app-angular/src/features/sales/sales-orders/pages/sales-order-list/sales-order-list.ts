import { Component } from '@angular/core';

/**
 * Placeholder — full implementation in PR 3.
 * Renders an empty container to satisfy lazy-route compilation.
 */
@Component({
  standalone: true,
  selector: 'app-sales-order-list',
  template: `<div data-testid="sales-order-list-placeholder"></div>`,
})
export class SalesOrderList {}
