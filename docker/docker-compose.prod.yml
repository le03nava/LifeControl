# ============================================
# Docker Compose - Production Configuration
# ============================================
# Use: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up
# Or use the provided scripts

# Network configuration - default bridge network
# Override with NETWORK_NAME in each service if needed
networks:
  default:
    driver: bridge

volumes:
  postgres_data:
    external: true
  prometheus_data:
    external: true
  grafana_data:
    external: true
  tempo_data:
    external: true
  loki_data:
    external: true

services:
  # ============================================
  # Production PostgreSQL - Keycloak
  # ============================================
  keycloak-postgres:
    image: ${POSTGRES_IMAGE:-postgres:18.1}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-keycloak-postgres
    ports:
      - "${POSTGRES_PORT:-5453}:5432"
    environment:
      POSTGRES_DB: ${KEYCLOAK_POSTGRES_DB:-keycloak_production}
      POSTGRES_USER: ${KEYCLOAK_POSTGRES_USER:-keycloak}
      POSTGRES_PASSWORD_FILE: ${KEYCLOAK_POSTGRES_PASSWORD_FILE}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - default
    deploy:
      replicas: 1
      resources:
        limits:
          memory: ${POSTGRES_MEMORY_LIMIT:-2g}
          cpus: "1.0"
        reservations:
          memory: 1g
          cpus: "0.5"
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5
        window: 180s
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${KEYCLOAK_POSTGRES_USER:-keycloak}"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    restart: always

  # ============================================
  # Production Keycloak
  # ============================================
  keycloak:
    image: ${KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:26.2.1}
    command: ["start", "--optimized"]
    ports:
      - "${KEYCLOAK_PORT:-8381}:8080"
      - "${KC_HTTPS_PORT:-8443}:8443"
    environment:
      KC_DB: ${KC_DB:-postgres}
      KC_DB_URL: ${KC_DB_URL:-jdbc:postgresql://keycloak-postgres/keycloak_production}
      KC_DB_USERNAME: ${KC_DB_USERNAME:-keycloak}
      KC_DB_PASSWORD_FILE: ${KC_DB_PASSWORD_FILE}
      KC_HOSTNAME: ${KC_HOSTNAME:-auth.lifecontrol.com}
      KC_HTTP_PORT: ${KC_HTTP_PORT:-8080}
      KC_HTTPS_PORT: ${KC_HTTPS_PORT:-8443}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_ADMIN_USERNAME:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD_FILE: ${KC_ADMIN_PASSWORD_FILE}
    volumes:
      - ${KEYCLOAK_REALMS_PATH:-/var/lib/lifecontrol/volumes/keycloak/realms}:/opt/keycloak/data/import:ro
    networks:
      - default
    depends_on:
      keycloak-postgres:
        condition: service_healthy
    deploy:
      replicas: 2
      resources:
        limits:
          memory: ${KEYCLOAK_MEMORY_LIMIT:-2g}
          cpus: "1.0"
        reservations:
          memory: 1g
          cpus: "0.5"
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 180s
    restart: always

  # ============================================
  # Production Monitoring Stack
  # ============================================
  loki:
    image: ${LOKI_IMAGE:-grafana/loki:2.9.0}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-loki
    command: ["-config.file=/etc/loki/local-config.yaml"]
    ports:
      - "${LOKI_PORT:-3300}:3100"
    volumes:
      - loki_data:/loki/data
    networks:
      - default
    deploy:
      resources:
        limits:
          memory: ${LOKI_MEMORY_LIMIT:-1g}
          cpus: "0.5"
    restart: always

  prometheus:
    image: ${PROMETHEUS_IMAGE:-prom/prometheus:v2.48.0}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-prometheus
    command:
      - --enable-feature=exemplar-storage
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=30d
      - --storage.tsdb.wal-compression
      - --web.enable-lifecycle
    ports:
      - "${PROMETHEUS_PORT:-9290}:9090"
    volumes:
      - ${PROMETHEUS_CONFIG_PATH:-/var/lib/lifecontrol/volumes/prometheus/config}:/etc/prometheus:ro
      - prometheus_data:/prometheus
    networks:
      - default
    deploy:
      resources:
        limits:
          memory: ${PROMETHEUS_MEMORY_LIMIT:-2g}
          cpus: "1.0"
    restart: always

  tempo:
    image: ${TEMPO_IMAGE:-grafana/tempo:2.3.0}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-tempo
    command: ["-config.file=/etc/tempo.yaml"]
    ports:
      - "${TEMPO_PORT:-3310}:3100"
      - "${TEMPO_ZIPKIN_PORT:-9611}:9411"
    volumes:
      - ${TEMPO_CONFIG_PATH:-/var/lib/lifecontrol/volumes/tempo/config}:/etc/tempo:ro
      - tempo_data:/var/tempo
    networks:
      - default
    deploy:
      resources:
        limits:
          memory: ${TEMPO_MEMORY_LIMIT:-2g}
          cpus: "1.0"
    restart: always

  grafana:
    image: ${GRAFANA_IMAGE:-grafana/grafana:10.2.0}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-grafana
    ports:
      - "${GRAFANA_PORT:-3200}:3000"
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: ${GF_AUTH_ANONYMOUS_ENABLED:-false}
      GF_AUTH_BASIC_ENABLED: ${GF_AUTH_BASIC_ENABLED:-true}
      GF_AUTH_DISABLE_LOGIN_FORM: ${GF_AUTH_DISABLE_LOGIN_FORM:-false}
      GF_SECURITY_ADMIN_USER: ${GF_SECURITY_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD__FILE: /run/secrets/grafana_admin_password
    volumes:
      - ${GRAFANA_CONFIG_PATH:-/var/lib/lifecontrol/volumes/grafana/config}:/etc/grafana/provisioning/datasources:ro
      - grafana_data:/var/lib/grafana
    networks:
      - default
    secrets:
      - grafana_admin_password
    deploy:
      resources:
        limits:
          memory: ${GRAFANA_MEMORY_LIMIT:-1g}
          cpus: "0.5"
    restart: always

  # ============================================
  # Production API Gateway
  # ============================================
  api-gateway:
    build:
      context: ../api-gateway
      dockerfile: Dockerfile
      args:
        BUILD_PROFILE: prod
        APP_VERSION: ${APP_VERSION:-1.0.0}
    ports:
      - "${API_GATEWAY_PORT:-9200}:${API_GATEWAY_SERVER_PORT:-9200}"
      - "${API_GATEWAY_MANAGEMENT_PORT:-9201}:${API_GATEWAY_MANAGEMENT_PORT:-9201}"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SERVER_PORT=${API_GATEWAY_SERVER_PORT:-9200}
      - MANAGEMENT_PORT=${API_GATEWAY_MANAGEMENT_PORT:-9201}
      - KEYCLOAK_ISSUER_URI=${KEYCLOAK_ISSUER_URI:-https://auth.lifecontrol.com/realms/life-control-realm}
      - LOKI_URL=${LOKI_URL:-http://loki:3300/loki/api/v1/push}
      - LOG_LEVEL=WARN
      - PROMETHEUS_ENABLED=true
      - TRACING_ENABLED=true
      - TRACING_SAMPLING_PROBABILITY=0.01
      - HEALTH_CHECK_ENABLED=true
      - HEALTH_CHECK_PATH=/actuator/health
      - CIRCUIT_BREAKER_ENABLED=true
      - CIRCUIT_BREAKER_FAILURE_THRESHOLD=50
      - TIMEOUT_DURATION=5s
      - RETRY_MAX_ATTEMPTS=3
    env_file:
      - .env.prod
      - .env.secrets
    networks:
      - default
    depends_on:
      keycloak:
        condition: service_healthy
    deploy:
      replicas: 3
      resources:
        limits:
          memory: ${API_GATEWAY_MEMORY_LIMIT:-2g}
          cpus: "1.0"
        reservations:
          memory: 1g
          cpus: "0.5"
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://localhost:${API_GATEWAY_SERVER_PORT:-9200}/actuator/health",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 120s
    restart: always

  # ============================================
  # Production PostgreSQL - LifeControl API
  # ============================================
  lifecontrol-postgres:
    image: ${LIFECONTROL_POSTGRES_IMAGE:-postgres:16.4}
    container_name: ${COMPOSE_PROJECT_NAME:-lifecontrol}-lifecontrol-postgres
    ports:
      - "${LIFECONTROL_POSTGRES_PORT:-5435}:5432"
    environment:
      POSTGRES_DB: ${LIFECONTROL_POSTGRES_DB:-lifecontrol}
      POSTGRES_USER: ${LIFECONTROL_POSTGRES_USER:-lifecontrol_user}
      POSTGRES_PASSWORD_FILE: ${LIFECONTROL_POSTGRES_PASSWORD_FILE}
    volumes:
      - ${LIFECONTROL_POSTGRES_DATA_PATH:-/var/lib/lifecontrol/volumes/postgres/lifecontrol/data}:/var/lib/postgresql/data
      - ${LIFECONTROL_POSTGRES_INIT_PATH:-/var/lib/lifecontrol/init-scripts}:/docker-entrypoint-initdb.d:ro
    networks:
      - default
    deploy:
      replicas: 1
      resources:
        limits:
          memory: ${LIFECONTROL_POSTGRES_MEMORY_LIMIT:-512m}
          cpus: "0.5"
        reservations:
          memory: 256m
          cpus: "0.25"
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5
        window: 180s
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${LIFECONTROL_POSTGRES_USER:-lifecontrol_user}",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    restart: always

# ============================================
# Production Web App - Angular Frontend
# ============================================
  web-app:
    build:
      context: ../life-control-app-angular
      dockerfile: Dockerfile
    image: ${WEB_APP_IMAGE:-life-control-app-angular:latest}
    ports:
      - "${WEB_APP_PORT:-4420}:3000"
    environment:
      - NODE_ENV=production
      - KEYCLOAK_URL=${KEYCLOAK_URL:-https://auth.lifecontrol.com}
      - KEYCLOAK_REALM=${KEYCLOAK_REALM:-life-control-realm}
      - KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID:-life-control-client}
      - API_GATEWAY_URL=${API_GATEWAY_URL:-http://api-gateway:9200}
      - API_BASE_PATH=${API_BASE_PATH:-/api}
    networks:
      - default
    depends_on:
      - api-gateway
      - keycloak
    deploy:
      replicas: 2
      resources:
        limits:
          memory: ${WEB_APP_MEMORY_LIMIT:-512m}
          cpus: "0.5"
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    restart: always

# ============================================
# Docker Secrets - Production
# ============================================
secrets:
  grafana_admin_password:
    file: ./secrets/grafana_admin_password.txt
