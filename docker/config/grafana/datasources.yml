# ============================================
# LifeControl - Grafana Datasource Provisioning
# ============================================
# Installed by setup-env.sh into $VOLUMES_ROOT/grafana/config (mounted at
# /etc/grafana/provisioning/datasources).
# ============================================
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100

  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3100