# LifeControl - Secret file template: keycloak_admin_password
# Copy to docker/secrets/keycloak_admin_password and replace the CHANGEME
# value with the real production Keycloak admin password. The file will be
# mounted read-only into the keycloak container at /run/secrets/.
#
# Do NOT commit the materialized secrets file (gitignored via docker/secrets/*).
# setup-env.sh prod copies this template (stripping comment lines) and chmod 0444.
CHANGEME
