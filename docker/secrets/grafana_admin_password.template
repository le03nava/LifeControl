# LifeControl - Secret file template: grafana_admin_password
# Copy to docker/secrets/grafana_admin_password and replace the CHANGEME
# value with the real production Grafana admin password. The file will be
# mounted read-only into the grafana container at /run/secrets/.
#
# Do NOT commit the materialized secrets file (gitignored via docker/secrets/*).
# setup-env.sh prod copies this template (stripping comment lines) and chmod 0444.
CHANGEME
