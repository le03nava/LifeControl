# LifeControl - Secret file template: lifecontrol_postgres_password
# Copy to docker/secrets/lifecontrol_postgres_password and replace the CHANGEME
# value with the real production DB password. The file will be mounted read-only
# into the lifecontrol-postgres container at /run/secrets/.
#
# Do NOT commit the materialized secrets file (gitignored via docker/secrets/*).
# setup-env.sh prod copies this template (stripping comment lines) and chmod 0444.
CHANGEME
