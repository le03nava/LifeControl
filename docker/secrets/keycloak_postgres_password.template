# LifeControl - Secret file template: keycloak_postgres_password
# Copy to docker/secrets/keycloak_postgres_password and replace the CHANGEME
# value with the real production password. The file will be mounted read-only
# into the keycloak and keycloak-postgres containers at /run/secrets/.
#
# Do NOT commit the materialized secrets file (gitignored via docker/secrets/*).
# setup-env.sh prod copies this template (stripping comment lines) and chmod 0444.
CHANGEME
